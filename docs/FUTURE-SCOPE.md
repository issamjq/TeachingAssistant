# Future scope

Ideas and features that are deliberately out of scope for the current
build. Add to this list instead of scope-creeping the current pipeline;
revisit once the core flow (see [00-concept.md](00-concept.md)) is solid.

- **Student self-serve learning materials** — right now students are
  invite-only and only see what a teacher schedules for them. A later phase
  could let students pull their own study materials or practice directly,
  independent of a teacher-initiated assignment.
