export default function TeacherOnboardingPage() {
  return (
    <main>
      <h1>Teacher onboarding</h1>
      <p>
        Institution name, staff ID (photo or number), syllabus type.
        Submitting puts the account into `pending` until a super_admin,
        sub_admin, or the named organisation approves it.
      </p>
    </main>
  );
}
