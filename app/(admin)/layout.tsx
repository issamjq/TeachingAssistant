// super_admin / sub_admin / organisation consoles. No role guard yet —
// this is a placeholder until the auth/role model (docs/00-concept.md)
// lands.
export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
