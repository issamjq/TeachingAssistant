export default function SupportPage() {
  return (
    <main>
      <h1>Support</h1>
      <p>
        Help screen plus the site-wide support assistant — full access
        across the teacher&apos;s own data, unlike the per-record Studio
        assistants elsewhere in the app.
      </p>
    </main>
  );
}
